{
  "version": 2,
  "builds": [
    {
      "src": "dist/survey-frontend/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
